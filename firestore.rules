
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // This rule allows anyone to read and write to the database.
    // It is insecure and should only be used for local development.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
